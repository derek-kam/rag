server.port=8080
server.host=0.0.0.0

# Oracle Database 23ai connection
# Override with environment variables for production use.
db.url=${DB_URL:jdbc:oracle:thin:@localhost:1521/FREEPDB1}
db.username=${DB_USERNAME:RAG_APP}
db.password=${DB_PASSWORD:RagPassword123}
